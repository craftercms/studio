(function (window) {
    'use strict';

    var crafter = { studio: { preview: {  } } };

    crafter.studio.preview.Topics = {
        "ALL": "*",
        "GUEST_CHECKIN": "GUEST_CHECKIN",
        "GUEST_CHECKOUT": "GUEST_CHECKOUT",
        "GUEST_SITE_LOAD": "GUEST_SITE_LOAD"
    };

    window.crafter = crafter;

}) (window);